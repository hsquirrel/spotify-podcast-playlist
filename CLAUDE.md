# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

A tool to create Spotify playlists that order podcast episodes in a custom way. Licensed under Apache 2.0.

## Tech Stack

This is a .NET project (based on the .gitignore configuration). No source code has been added yet.

## Build Commands

Once source code is added, standard .NET commands apply:
- `dotnet build` — build the project
- `dotnet test` — run tests
- `dotnet run` — run the application
